<section class="border-y border-hairline bg-carriage/60">
	<div class="mx-auto max-w-6xl px-6 py-20 md:py-24">
		<p class="font-display text-sm tracking-widest text-signal uppercase">Two doors</p>
		<h2 class="mt-5 max-w-3xl font-display text-3xl leading-tight font-medium md:text-5xl">
			The screen and the agent open the same door, with the same key.
		</h2>
		<div class="mt-8 grid gap-8 md:grid-cols-2 md:gap-12">
			<div class="flex flex-col gap-3">
				<h3 class="font-display text-xl font-medium">Dashboards</h3>
				<p class="text-lg text-chalk/70">
					Everything in the harness is on a screen, like any other software: a list of the
					projects at the stage they are really at, the invoices that are due, the documents
					that arrived this week. People who like a browser use the browser.
				</p>
			</div>
			<div class="flex flex-col gap-3">
				<h3 class="font-display text-xl font-medium">The MCP server</h3>
				<p class="text-lg text-chalk/70">
					The same actions the website performs, reachable from inside Claude or ChatGPT.
					Ask what is in flight, raise a request, approve the invoice, run the month-end,
					without opening the site. It is the same connection we use to run the work.
				</p>
			</div>
		</div>
		<p class="mt-10 max-w-3xl border-t border-hairline pt-8 text-chalk/70">
			<span class="font-display text-chalk">One set of roles governs both.</span> Role-based access
			decides who may see and do what, and it is enforced on the MCP server exactly as it is on
			the screen. A person who cannot approve an invoice on the dashboard cannot approve it
			through their agent either.
		</p>
	</div>
</section>
